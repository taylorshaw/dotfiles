# more git customization docs info in git manual (man git-config)
# and Pro Git book here:
# https://git-scm.com/book/en/v2

[user]
	name = Taylor Shaw
	email = taylor@taylorshaw.net
[credential]
	helper = osxkeychain
[color]
	ui = auto
	branch = auto
	status = auto
	diff = false
[core]
	editor = emacs -nw
	excludesfile = ~/.gitignore
# [diff]
#	external = opendiff
	
